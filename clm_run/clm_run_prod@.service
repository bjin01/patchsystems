[Unit]
Description=SUSE Manager - content lifecycle management promote stage for project %i
OnFailure=clm_run_failed@%n.service

[Service]
# The PROJ is a variable and should get the instance name of the service which in turn is the clm project label.
# the value for --envLabel should be the environment before prod, if test -> prod 
ExecStart=/usr/bin/mgrctl exec "python3.6 /root/scripts/clm_run.py --promote --projLabel $PROJ --envLabel test" -e PROJ=%i
